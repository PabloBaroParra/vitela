# macOS shell (stub)

Placeholder for the SwiftUI application. Not a Rust crate — not part of the
Cargo workspace.

Implementation lands in Batch 9 (T-055..T-059): SwiftUI app consuming the
`pdf-ffi` UniFFI-generated Swift bindings for open/render/scroll/zoom,
password prompt, text select/search, annotation toolbar, undo/redo,
`NSPrintOperation` printing, `NSPasteboard` paste + drag-and-drop, and
`.dylib` bundling with sign/notarize wired into `macos.yml`.
